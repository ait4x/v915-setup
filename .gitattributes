# Batch files must keep CRLF endings; cmd.exe mis-parses lone LF in some
# constructs. Everything else is normalised to LF in the repo.
* text=auto eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.psd1 text eol=crlf
